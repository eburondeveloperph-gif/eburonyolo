{
  "entities": {
    "enrollment": {
      "title": "Biometric Enrollment",
      "description": "A record of a user's biometric facial scan and name.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "The full name of the enrolled subject."
        },
        "photo": {
          "type": "string",
          "description": "Base64 encoded JPEG image of the subject's face."
        },
        "timestamp": {
          "type": "number",
          "description": "The Unix timestamp when the enrollment was created."
        },
        "authorUid": {
          "type": "string",
          "description": "The Firebase Auth UID of the operator who created this enrollment."
        }
      },
      "required": ["name", "photo", "timestamp", "authorUid"]
    }
  },
  "firestore": {
    "/enrollments/{enrollmentId}": {
      "schema": "enrollment",
      "description": "Collection of all biometric identities enrolled in the system."
    }
  }
}
